import { Feather } from '@expo/vector-icons';
import { router } from 'expo-router';
import React, { useState } from 'react';
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from 'react-native';
import { AppHeader, Button, ChoiceRow, Field, Pill, Screen, SectionTitle } from '@/components/ui';
import { useApp } from '@/context/AppContext';
import { useColors } from '@/hooks/useColors';

export default function JobPrep() {
  const colors = useColors();
  const { setConfig } = useApp();
  const [company, setCompany] = useState('Google');
  const [jd, setJd] = useState('We are looking for a Flutter engineer to build reliable, delightful mobile experiences. You will work with Dart, Firebase, REST APIs, and a cross-functional product team.');
  const [analyzing, setAnalyzing] = useState(false);
  const [analyzed, setAnalyzed] = useState(false);
  const analyze = () => {
    setAnalyzing(true);
    setTimeout(() => { setAnalyzing(false); setAnalyzed(true); }, 900);
  };
  return <Screen><AppHeader title="Prepare for a job" subtitle="Turn a job description into a plan" onBack={() => router.back()} /><View style={[styles.hero, { backgroundColor: colors.navy }]}><View style={[styles.heroIcon, { backgroundColor: colors.mint }]}><Feather name="briefcase" size={22} color={colors.navy} /></View><Text style={styles.heroTitle}>Practice for the role, not just the question.</Text><Text style={styles.heroBody}>We’ll compare the role requirements with your resume and highlight the gaps worth practicing.</Text></View><SectionTitle title="Target company" /><View style={styles.companyRow}>{['Google', 'Microsoft', 'Startup'].map((item) => <Pressable key={item} onPress={() => setCompany(item)} style={[styles.company, { backgroundColor: company === item ? colors.primary : colors.card, borderColor: company === item ? colors.primary : colors.border }]}><Text style={[styles.companyText, { color: company === item ? colors.primaryForeground : colors.foreground }]}>{item}</Text></Pressable>)}</View><SectionTitle title="Job description" /><Text style={[styles.helper, { color: colors.mutedForeground }]}>Paste the role you’re targeting. This mock flow uses local sample analysis.</Text><Field value={jd} onChangeText={setJd} placeholder="Paste a job description…" multiline />{analyzing ? <View style={[styles.analyzing, { backgroundColor: colors.secondary }]}><ActivityIndicator color={colors.primary} /><Text style={[styles.analyzingText, { color: colors.foreground }]}>Analyzing role requirements…</Text></View> : <Button label={analyzed ? 'Re-analyze job description' : 'Analyze job description'} icon="search" onPress={analyze} />}{analyzed ? <><SectionTitle title="Role snapshot" /><View style={[styles.snapshot, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={styles.snapshotTop}><Text style={[styles.snapshotTitle, { color: colors.foreground }]}>Flutter Engineer</Text><Pill label="Good match" tone="success" /></View><Text style={[styles.snapshotText, { color: colors.mutedForeground }]}>Your resume matches 4 of 6 core skills for this role.</Text><View style={styles.skillWrap}>{['Flutter', 'Dart', 'Firebase', 'REST APIs'].map((skill) => <Pill key={skill} label={skill} tone="success" />)}{['Clean Architecture', 'Testing'].map((skill) => <Pill key={skill} label={`${skill} · gap`} tone="coral" />)}</View><View style={[styles.gap, { backgroundColor: colors.secondary }]}><Feather name="compass" size={16} color={colors.primary} /><View style={styles.gapCopy}><Text style={[styles.gapTitle, { color: colors.foreground }]}>Recommended focus</Text><Text style={[styles.gapText, { color: colors.mutedForeground }]}>Practice Clean Architecture before your next interview.</Text></View></View><Button label="Practice this role" icon="arrow-right" onPress={() => { setConfig({ role: 'Flutter Developer', company }); router.push('/create'); }} /></View></> : null}<Pressable onPress={() => setAnalyzed(false)} style={styles.uploadLink}><Feather name="upload" size={14} color={colors.primary} /><Text style={[styles.uploadText, { color: colors.primary }]}>Upload a job description instead</Text></Pressable></Screen>;
}

const styles = StyleSheet.create({ hero: { borderRadius: 22, padding: 19, marginTop: 12 }, heroIcon: { width: 44, height: 44, borderRadius: 15, alignItems: 'center', justifyContent: 'center', marginBottom: 14 }, heroTitle: { color: '#FFFFFF', fontFamily: 'Inter_700Bold', fontSize: 22, lineHeight: 27, maxWidth: 290 }, heroBody: { color: '#BFCBE5', fontFamily: 'Inter_400Regular', fontSize: 11, lineHeight: 17, marginTop: 8 }, companyRow: { flexDirection: 'row', gap: 8 }, company: { borderRadius: 14, borderWidth: 1, paddingHorizontal: 14, paddingVertical: 11 }, companyText: { fontFamily: 'Inter_600SemiBold', fontSize: 12 }, helper: { fontFamily: 'Inter_400Regular', fontSize: 11, lineHeight: 17, marginTop: -3, marginBottom: 10 }, analyzing: { minHeight: 52, borderRadius: 16, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 10 }, analyzingText: { fontFamily: 'Inter_600SemiBold', fontSize: 12 }, snapshot: { borderRadius: 20, borderWidth: 1, padding: 16 }, snapshotTop: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, snapshotTitle: { fontFamily: 'Inter_700Bold', fontSize: 16 }, snapshotText: { fontFamily: 'Inter_400Regular', fontSize: 11, marginTop: 8, marginBottom: 13 }, skillWrap: { flexDirection: 'row', gap: 7, flexWrap: 'wrap' }, gap: { borderRadius: 15, padding: 12, flexDirection: 'row', marginTop: 17, marginBottom: 16 }, gapCopy: { flex: 1, marginLeft: 9 }, gapTitle: { fontFamily: 'Inter_600SemiBold', fontSize: 11 }, gapText: { fontFamily: 'Inter_400Regular', fontSize: 10, lineHeight: 15, marginTop: 3 }, uploadLink: { flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 7, marginTop: 22, marginBottom: 15 }, uploadText: { fontFamily: 'Inter_600SemiBold', fontSize: 11 } });